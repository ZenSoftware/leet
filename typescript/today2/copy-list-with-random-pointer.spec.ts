import { copyRandomList, _Node } from './copy-list-with-random-pointer';

describe('Copy List with Random Pointer', () => {
  it('evaluates correctly', () => {
    expect(true).toEqual(true);
  });
});
